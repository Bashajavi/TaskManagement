# Application name
spring.application.name=Task-Manager

# Database connection details
spring.datasource.url=jdbc:mysql://mysql-d5558b8-bashajavi-1c47.c.aivencloud.com:18034/defaultdb
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_AJ3q0MP-ShZggrsQni5

# Connection pool settings (HikariCP is the default connection pool in Spring Boot)
spring.datasource.hikari.maximum-pool-size=10        # Adjust based on expected concurrent requests
spring.datasource.hikari.minimum-idle=5             # Minimum number of idle connections
spring.datasource.hikari.idle-timeout=30000         # Time (in ms) before idle connections are removed
spring.datasource.hikari.max-lifetime=1800000       # Maximum lifetime (in ms) of a connection
spring.datasource.hikari.connection-timeout=30000   # Timeout (in ms) for a connection from the pool

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=validate              # Set to 'validate' or 'none' for production
spring.jpa.show-sql=false                           # Disable SQL logging in production
spring.jpa.properties.hibernate.format_sql=false    # Avoid formatting SQL for production
spring.jpa.properties.hibernate.use_sql_comments=false # Disable SQL comments for production
spring.jpa.properties.hibernate.jdbc.batch_size=30  # Enable batching for better insert/update performance
spring.jpa.properties.hibernate.order_inserts=true  # Optimize batch inserts
spring.jpa.properties.hibernate.order_updates=true  # Optimize batch updates
spring.jpa.properties.hibernate.cache.use_second_level_cache=true
spring.jpa.properties.hibernate.cache.use_query_cache=true
spring.jpa.properties.hibernate.cache.region.factory_class=org.hibernate.cache.ehcache.EhCacheRegionFactory

# Server port
server.port=8080

# Lazy initialization for faster startup
spring.main.lazy-initialization=true

# Logging configuration
logging.level.root=INFO
logging.file.name=logs/application.log
